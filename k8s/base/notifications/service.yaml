apiVersion: v1
kind: Service
metadata:
  name: trunk-notifications
  labels:
    app: trunk-notifications
    project: trunk
spec:
  type: ClusterIP
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: trunk-notifications
